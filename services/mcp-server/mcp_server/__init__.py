"""Trifecta MCP server — Signal backend.

Exposes the Phase 0 MMM tools over the Model Context Protocol, grounded in the
fitted Meridian model's posterior outputs (results.json). Every tool returns
values with credible intervals — never point estimates dressed as certainty.
"""
