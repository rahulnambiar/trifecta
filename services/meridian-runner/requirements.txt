# Meridian + the training/analysis stack it pulls in (TensorFlow Probability, etc.)
google-meridian>=1.3,<2
# Artifact upload + Vertex AI job submission
google-cloud-storage>=2.16
google-cloud-aiplatform>=1.60
# Data handling (also transitively required by Meridian)
pandas>=2.0
numpy>=1.26
xarray>=2024.0
