'use client';
// NeuGenM distributor white-label. Renders the same app; the brand is resolved from
// the /neugenm path (see lib/brand.js). UI only — same models, data, and accounts.
import App from '../page';

export default function NeuGenM() {
  return <App />;
}
